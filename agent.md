# agent.md —— 本仓库写作与内容管理规则

> 本文件是给 AI 助手和未来维护者看的内容管理规范。改动分类/标签体系前必读；与本文件冲突的历史做法一律以本文件为准。

## 一、项目速览

- Hexo 8 博客，主题 Butterfly 5.7（npm 安装），GitHub Actions 自动部署到 GitHub Pages（自定义域名 bohuyeshan.top）

- 文章目录：`source/_posts/*.md`，文件名格式 `YYYY-MM-DD-NN-标题-连字符分隔.md`（同日多篇用 NN 递增）

- 构建验证：`npx hexo generate`；本地预览：`npx hexo server`

- 第三方前端库已全量本地自托管（`CDN.third_party_provider: local`），**不要在文章或配置里引入 jsdelivr/cdnjs 外链**

## 二、分类体系（权威定义，共 7 类）

分类管「栏目」，一篇文章只属于一个分类。

| 分类     | 收录标准                        |
| ------ | --------------------------- |
| `生信技术` | 生物信息学方法、管线、工具、工作站等技术向内容（主桶） |
| `科普`   | 面向大众读者的科学科普文（含原公众号同步稿）      |
| `科研`   | 科研经历、学术思考、职业思考、留档记录         |
| `AI`   | AI/大模型/Agent 主题的方法论与认知类文章   |
| `周报`   | 每周资讯周报系列                    |
| `技术拆解` | 对具体项目/系统/架构的深度拆解分析          |
| `工具教程` | 游戏、软件等非科研类实用教程（如 Steam 系列）  |

**禁止**：

- 不新造单篇专用分类（历史遗留的 `路标篇`、`归档`、`博客` 已清理）

- 不按来源划分分类（`公众号文章` 已取消，来源用正文说明即可）

- 不使用多级嵌套分类（历史遗留的 `- - steam` 嵌套写法已拍平）

## 三、标签规范

标签管「主题」，用于检索和关联。分类已经表达的信息不要用标签重复。

### 硬性规则

1. **禁用与分类同名的标签**：栏目名不得再作为标签出现（存量已于 2026-08-23 清洗）
2. **一篇 3–6 个标签**为宜；没有准确主题标签时宁缺毋滥，不为凑数发明一次性标签
3. **优先复用已有标签**；确需新建时遵循同一命名风格（中文主题用中文，专有名词/语言/产品保留英文原名如 Rust、Claude、Zeta）
4. 同义概念只允许用映射表右侧的**权威形式**，左侧变体禁止新增
5. front-matter 分类/标签统一用列表写法（每行一个 `- 条目`），禁止嵌套列表和行内数组

### 同义词映射表（左=禁用变体 → 右=唯一权威形式）

| 权威标签    | 禁用变体                  |
| ------- | --------------------- |
| `大模型`   | LLM、人工智能、智能           |
| `Agent` | AI-Agent、Coding Agent |
| `蛋白质`   | 蛋白质预测、蛋白质设计           |
| `数据工程`  | 数据清洗                  |
| `蛋白质结构` | 结构                    |

### 现存高频标签参考

`AI`(16)、`蛋白质`(12)、`Agent`(10)、`蛋白质结构`(8)、`大模型`(6)、`单细胞`(5)、`深度学习`(4)、`基因`/`基因编辑`(各3)、`Rust`(3)

### 系列专属分类与标签

- 「生信图表大全」系列（长期连载）：分类固定 `生信图表大全`、系列标签固定 `图表大全`，标题用「生信图表大全（第X篇）：…」编号，篇间互链用文件名 slug 中的「第X篇」

## 四、Front-matter 模板

```yaml
---
title: 文章标题
date: 2026-08-23
published: true
tags:
  - 蛋白质
  - 大模型
categories:
  - 生信技术
---
```

- 仅当正文确有公式才加 `katex: true`

- 数学公式注意：根号内放求和/积分时写 `\sum\nolimits_{}` / `\int\nolimits_{}`，避免展示模式上下限把根号撑高

- Mermaid 图必须用主题标签语法，**代码围栏会被高亮器吞掉**：

```
{% mermaid %}
flowchart LR
    A --> B
{% endmermaid %}
```

- 正文可直接内联 `<svg>`，构建管线会原样透传

- 不想公开又想留在仓库的文章：front-matter 加 `published: false`

## 五、正文 Markdown 渲染规范（防强调/链接渲染失败）

> 渲染器遵循 CommonMark 侧翼规则：`**` 是"开启"还是"闭合"由两侧字符决定，**中文标点属于 Unicode 标点，会干扰判定**。同一种写法有时能加粗、有时不能，不是玄学——位置决定结果，因此按下述规范写可完全预防。

### 硬性规则

1. **中文标点一律放在强调符号外侧**。适用于 `**`、`*`、`***`、`~~`、`__` 全部强调记号。

   ```text
   ✅ **第一，把审定流程做快做专业**。锐评说
   ❌ **第一，把审定流程做快做专业。**锐评说
   ```

   原理：`。**锐` 中夹在标点与汉字之间的 `**` 会被判定为新的"开启"，前一个 `**` 找不到闭合，整段不加粗、星号原样输出。注意 `**xxx。**` 出现在**段落末尾**时恰好能正常渲染——这正是不规范写法时而生效的原因；不要依赖这种位置侥幸。

2. **链接 URL 禁止含空格与全角标点**（`：，。？！（）` 等）。含空格的行内链接无法解析，整条链接会按纯文本输出（斜杠被转义成 `&#x2F;` 是其典型特征）。

   ```text
   ✅ [《别让 AI 心算》](/2026/08/22/2026-08-22-01-别让AI心算-你以为在测它-其实只是没给草稿纸/)
   ❌ [《别让 AI 心算》](/2026/08/22/别让 AI 心算：你以为在测它，其实只是没给草稿纸/)
   ```

   站内链接一律用目标文章的真实永久路径（即源文件名，连字符分隔、无空格无全角）；外链含特殊字符时用尖括号形式 `<https://...>`。

3. **子列表缩进必须与父项文字对齐**（无序列表下 ≥2 空格，有序列表下 ≥3 空格）。缩进不足时子列表会被吸进上一条目末尾当纯文本。

4. **表格单元格内禁用裸** **`|`**，必要时写 `\|`；表格必须有 `| --- |` 分隔行。

5. **行内代码含反引号时用双反引号包裹**：`` `code` `` → ` `  `code` ` ` 。

### 兜底与自检（两道防线）

| 防线      | 机制                            | 说明                                               |
| ------- | ----------------------------- | ------------------------------------------------ |
| 渲染前自动修复 | `scripts/fix-cjk-emphasis.js` | Hexo `before_post_render` 过滤器，自动修复规则 1 的强调问题，已常驻 |
| 构建后审计   | `scripts/scan-html.js`        | 扫描 `public/` 全部文章页，检测未渲染的星号/链接/删除线/井号残留          |

新文章发布前跑一次 `node scripts/scan-html.js`，输出应为 `疑似问题 0 处`。

## 六、变更须知

- 改分类/标签后 URL 会变化（Hexo 按名称生成路径），旧链接会 404，站小可接受；改完必须重新构建部署

- 批量清理/合并标签前，先跑统计脚本核对分布，改完复核一遍计数

- 注意历史遗留的三种 front-matter 写法：标准列表、嵌套列表（`- - xxx`）、行内数组（`[]`），批量处理脚本必须三者都兼容

